好的，既然你要把现有的个人网站作品集页面，**彻底改造**成我们讨论的这种「长滚动全屏展示 + 自动化加载 + 可交互 UI 模拟器」的高级形态，我为你写一份**完整的、可直接执行的改造方案（Markdown 格式）**。

这份方案会基于你现有的 `lanzhengpeng.github.io` 项目结构（React + Vite）来设计，告诉你每一步该做什么、改哪些文件、加哪些文件，以及如何迁移你现有的 `works/webrtc-live` 项目。

---

# Works 页面改造方案：长滚动展示 + 交互式项目模拟器

## 1. 改造目标

- **展示形式升级**：从普通列表页改为**全屏滚动（Scroll-snap）**，每屏一个项目，滚动自动吸附。
- **增加交互预览**：每个项目右侧（或下方）用 **手机/浏览器模拟器** 嵌入 `preview.html`，让访客可以**直接操作**你的项目 UI（而非只看截图）。
- **数据完全分离**：以后新增项目，只需在 `public/works-data/` 下新建文件夹并放入资源，运行一行脚本即可，**完全不用改 React 代码**。
- **体验优化**：加载时显示骨架屏，无数据时显示美观的空状态引导。

---

## 2. 新旧架构对比

| 对比项 | 旧方案 | 新方案 |
|--------|--------|--------|
| 数据源 | `src/data/works.js`（硬编码） | `public/works-data/manifest.json`（自动生成） |
| 项目资源 | 分散在 `works/`、`public/` 等 | 统一放在 `public/works-data/项目名/` |
| 预览形式 | 可能只有截图或链接 | **交互式 iframe 预览**（可点击/输入） |
| 展示布局 | 普通列表或网格 | **全屏滚动吸附**（PPT 式沉浸体验） |
| 添加项目 | 需要改 JS 代码，重新编译 | 只需建文件夹 + 运行脚本 |

---

## 3. 目录结构变更

```
lanzhengpeng.github.io/
├── public/
│   ├── works-data/              ← 🆕 新增：所有项目资源集中地
│   │   ├── manifest.json        ← 自动生成，禁止手动编辑
│   │   ├── chat-assistant/      ← 示例项目1（AI聊天）
│   │   │   ├── meta.json
│   │   │   └── preview.html     ← 🌟 核心：可交互的UI预览
│   │   └── webrtc-live/         ← 📦 从原 /works/webrtc-live 迁移至此
│   │       ├── meta.json
│   │       └── preview.html
│   ├── files/ ...               (保留原样)
│   └── images/ ...              (保留原样)
├── scripts/
│   └── generate-works.js        ← 🆕 新增：扫描生成清单脚本
├── src/
│   ├── pages/
│   │   └── Works.jsx            ← ✏️ 大幅改造
│   ├── styles/pages/
│   │   └── works.css            ← ✏️ 完全重写
│   └── data/
│       └── works.js             ← 🗑️ 可废弃或保留作备用
└── package.json                 ← ✏️ 添加 npm script
```

---

## 4. 改造步骤详解

### 4.1 第一步：新增自动扫描脚本

创建 `scripts/generate-works.js`，用于遍历 `public/works-data/` 生成清单文件。

```javascript
// scripts/generate-works.js
const fs = require('fs');
const path = require('path');

const dataDir = path.join(__dirname, '../public/works-data');
const outputFile = path.join(dataDir, 'manifest.json');

if (!fs.existsSync(dataDir)) {
  fs.mkdirSync(dataDir, { recursive: true });
}

const projects = fs.readdirSync(dataDir)
  .filter(f => {
    const fullPath = path.join(dataDir, f);
    return fs.statSync(fullPath).isDirectory() && f !== 'manifest.json';
  })
  .map(folder => {
    const folderPath = path.join(dataDir, folder);
    const metaPath = path.join(folderPath, 'meta.json');
    const previewPath = path.join(folderPath, 'preview.html');
    const coverPath = path.join(folderPath, 'cover.png');

    let meta = {};
    if (fs.existsSync(metaPath)) {
      meta = JSON.parse(fs.readFileSync(metaPath, 'utf-8'));
    }

    return {
      id: folder,
      title: meta.title || folder,
      subtitle: meta.subtitle || '',
      description: meta.description || '',
      techStack: meta.techStack || [],
      displayType: meta.displayType || 'browser', // 'mobile' 或 'browser'
      demoUrl: meta.demoUrl || '',
      githubUrl: meta.githubUrl || '',
      previewUrl: fs.existsSync(previewPath) ? `/works-data/${folder}/preview.html` : '',
      coverImage: fs.existsSync(coverPath) ? `/works-data/${folder}/cover.png` : ''
    };
  });

fs.writeFileSync(outputFile, JSON.stringify(projects, null, 2));
console.log(`✅ 已生成 manifest.json，共 ${projects.length} 个项目。`);
```

然后在 `package.json` 的 `scripts` 中添加：

```json
"scripts": {
  "generate-works": "node scripts/generate-works.js"
}
```

---

### 4.2 第二步：创建项目资源模板

在 `public/works-data/` 下为每个项目建立文件夹，必须包含 `meta.json` 和 `preview.html`。

**meta.json 模板：**

```json
{
  "title": "项目中文名称",
  "subtitle": "英文副标题或简短定位",
  "description": "详细描述，支持多行文字。说明项目解决了什么问题，用了什么技术。",
  "techStack": ["React", "Node.js", "MongoDB", "WebSocket"],
  "displayType": "browser",
  "demoUrl": "https://demo.example.com",
  "githubUrl": "https://github.com/yourname/repo"
}
```

**preview.html 模板（带交互示例）：**

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>项目预览</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      font-family: system-ui, sans-serif;
      background: #f8fafc;
      height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }
    .demo-card {
      background: white;
      padding: 30px 40px;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.06);
      text-align: center;
      max-width: 400px;
      width: 100%;
    }
    .demo-card h2 { margin-bottom: 12px; color: #0f172a; }
    .demo-card p { color: #475569; margin-bottom: 20px; line-height: 1.6; }
    .demo-card button {
      padding: 10px 28px;
      background: #2563eb;
      color: white;
      border: none;
      border-radius: 40px;
      font-size: 0.9rem;
      cursor: pointer;
      transition: all 0.2s;
    }
    .demo-card button:hover { background: #1d4ed8; transform: scale(1.02); }
    .demo-card button:active { transform: scale(0.96); }
    .demo-card .counter {
      margin-top: 16px;
      font-size: 0.85rem;
      color: #94a3b8;
    }
  </style>
</head>
<body>
  <div class="demo-card">
    <h2>🚀 项目交互演示</h2>
    <p>点击下方按钮，体验真实的交互反馈。</p>
    <button id="clickBtn">点我试试</button>
    <div class="counter" id="counterDisplay">已点击 0 次</div>
  </div>

  <script>
    let count = 0;
    const btn = document.getElementById('clickBtn');
    const display = document.getElementById('counterDisplay');
    btn.addEventListener('click', () => {
      count++;
      display.textContent = `已点击 ${count} 次`;
      // 模拟震动反馈（移动端支持）
      if (navigator.vibrate) navigator.vibrate(10);
    });
  </script>
</body>
</html>
```

---

### 4.3 第三步：迁移现有项目（以 `webrtc-live` 为例）

你现有的 `works/webrtc-live/` 是一个独立页面，我们可以将它直接迁移为新格式：

1. **移动位置**：
   ```bash
   mv works/webrtc-live public/works-data/webrtc-live
   ```

2. **创建 meta.json**（放入该文件夹内）：
   ```json
   {
     "title": "WebRTC 实时互动",
     "subtitle": "点对点音视频通信演示",
     "description": "基于 WebRTC 的实时音视频通信应用，支持信令交换、P2P 连接和屏幕共享。",
     "techStack": ["WebRTC", "React", "Socket.io", "Node.js"],
     "displayType": "browser",
     "demoUrl": "https://webrtc-demo.example.com",
     "githubUrl": "https://github.com/lanzhengpeng/webrtc-live"
   }
   ```

3. **调整路径**：如果原 `index.html` 中引用了外部资源（如 `/images/xxx.png`），确保使用绝对路径。如果原本就是独立完整的页面，直接将其重命名为 `preview.html` 即可。

---

### 4.4 第四步：改造 `src/pages/Works.jsx`

完全替换为以下新代码，实现数据加载、骨架屏、空状态、全屏滚动渲染。

```jsx
// src/pages/Works.jsx
import { useEffect, useState } from 'react';
import '../styles/pages/works.css';

// 骨架卡片
const SkeletonCard = () => (
  <div className="skeleton-section">
    <div className="skeleton-content">
      <div className="skeleton-info">
        <div className="skeleton-title"></div>
        <div className="skeleton-subtitle"></div>
        <div className="skeleton-desc"></div>
        <div className="skeleton-tags"><span></span><span></span><span></span></div>
        <div className="skeleton-actions"><span></span><span></span></div>
      </div>
      <div className="skeleton-mockup"><div className="skeleton-browser"></div></div>
    </div>
  </div>
);

export default function Works() {
  const [projects, setProjects] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetch('/works-data/manifest.json')
      .then(res => {
        if (!res.ok) throw new Error('manifest not found');
        return res.json();
      })
      .then(data => {
        setProjects(data);
        setLoading(false);
      })
      .catch(() => {
        setProjects([]);
        setLoading(false);
      });
  }, []);

  // 加载中：显示骨架屏
  if (loading) {
    return (
      <div className="works-container skeleton-container">
        <SkeletonCard /><SkeletonCard /><SkeletonCard />
      </div>
    );
  }

  // 空状态：引导添加项目
  if (!projects.length) {
    return (
      <div className="works-empty-state">
        <div className="empty-icon">📭</div>
        <h2>还没有作品</h2>
        <p>请在 <code>public/works-data/</code> 下创建项目文件夹，包含 <code>meta.json</code> 和 <code>preview.html</code>，然后运行 <code>npm run generate-works</code>。</p>
        <div className="empty-example">
          <pre>{`public/works-data/\n  └── my-project/\n      ├── meta.json\n      └── preview.html`}</pre>
        </div>
      </div>
    );
  }

  // 正常渲染
  return (
    <div className="works-container">
      {projects.map((project, index) => {
        const isMobile = project.displayType === 'mobile';
        const tags = (project.techStack || []).map(t => <span key={t}>{t}</span>);

        return (
          <section key={project.id} className="works-section">
            <div className="works-content">
              <div className="works-info">
                <h1>{project.title}</h1>
                {project.subtitle && <div className="works-subtitle">{project.subtitle}</div>}
                <p className="works-desc">{project.description}</p>
                <div className="works-tech">{tags}</div>
                <div className="works-actions">
                  {project.demoUrl && <a href={project.demoUrl} target="_blank" rel="noopener noreferrer">🔗 在线体验</a>}
                  {project.githubUrl && <a href={project.githubUrl} target="_blank" rel="noopener noreferrer" className="github">🐙 源码</a>}
                </div>
              </div>
              <div className="works-mockup">
                {project.previewUrl ? (
                  isMobile ? (
                    <div className="phone-frame">
                      <div className="phone-notch"></div>
                      <div className="phone-screen">
                        <iframe src={project.previewUrl} loading="lazy" title={project.title}></iframe>
                      </div>
                    </div>
                  ) : (
                    <div className="browser-frame">
                      <div className="browser-bar">
                        <div className="browser-dots"><span></span><span></span><span></span></div>
                        <div className="browser-url">{project.demoUrl || 'localhost'}</div>
                      </div>
                      <div className="browser-body">
                        <iframe src={project.previewUrl} loading="lazy" title={project.title}></iframe>
                      </div>
                    </div>
                  )
                ) : (
                  <div className="mockup-placeholder">📄 请添加 preview.html</div>
                )}
              </div>
            </div>
            <div className="works-page-num">{index + 1} / {projects.length}</div>
          </section>
        );
      })}
    </div>
  );
}
```

---

### 4.5 第五步：重写 `src/styles/pages/works.css`

完全替换为新的全屏滚动 + 模拟器 + 骨架样式（精简版，可直接复制使用）。

```css
/* src/styles/pages/works.css */

/* --- 容器 & 滚动吸附 --- */
.works-container {
  height: 100vh;
  overflow-y: scroll;
  scroll-snap-type: y mandatory;
  scroll-behavior: smooth;
  background: #0b0d10;
  color: #e8edf3;
}
.works-section {
  height: 100vh;
  scroll-snap-align: start;
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 2rem 5%;
  background: #0b0d10;
  position: relative;
}
.works-section:nth-child(odd) { background: #111419; }

/* --- 内容布局 --- */
.works-content {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  align-items: center;
  gap: 2.5rem;
  max-width: 1300px;
  width: 100%;
  height: 85vh;
}

/* --- 左侧信息 --- */
.works-info {
  flex: 1 1 35%;
  min-width: 280px;
  max-width: 500px;
}
.works-info h1 {
  font-size: clamp(2rem, 4vw, 3.2rem);
  background: linear-gradient(135deg, #f0f4ff, #7aa9ff);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  margin-bottom: 0.3rem;
}
.works-subtitle { font-size: 1.2rem; color: #94a3b8; margin-bottom: 0.8rem; }
.works-desc { color: #cbd5e1; line-height: 1.7; margin-bottom: 1rem; }
.works-tech { display: flex; flex-wrap: wrap; gap: 0.4rem 0.6rem; margin-bottom: 1.2rem; }
.works-tech span { background: #1e293b; padding: 0.2rem 0.9rem; border-radius: 16px; font-size: 0.75rem; color: #b9d0ff; border: 1px solid #334155; }
.works-actions a {
  display: inline-block; padding: 0.5rem 1.4rem; border-radius: 40px; font-weight: 500;
  text-decoration: none; background: #2563eb; color: #fff; margin-right: 0.6rem; font-size: 0.9rem;
  transition: all 0.2s;
}
.works-actions a:hover { transform: scale(1.03); opacity: 0.9; }
.works-actions a.github { background: transparent; color: #cbd5e1; border: 1px solid #475569; }

/* --- 右侧模拟器 --- */
.works-mockup {
  flex: 1 1 50%;
  min-width: 320px;
  max-width: 750px;
  height: 100%;
  max-height: 650px;
  display: flex;
  justify-content: center;
  align-items: center;
}
/* 浏览器外壳 */
.browser-frame {
  width: 100%; height: 100%; background: #1e293b; border-radius: 16px;
  border: 1px solid #334155; box-shadow: 0 20px 60px rgba(0,0,0,0.6);
  display: flex; flex-direction: column; overflow: hidden;
}
.browser-bar { padding: 10px 16px; background: #0f172a; display: flex; align-items: center; gap: 10px; flex-shrink: 0; }
.browser-dots { display: flex; gap: 6px; }
.browser-dots span { width: 12px; height: 12px; border-radius: 50%; display: block; }
.browser-dots span:nth-child(1) { background: #ef4444; }
.browser-dots span:nth-child(2) { background: #f59e0b; }
.browser-dots span:nth-child(3) { background: #22c55e; }
.browser-url { flex: 1; background: #1e293b; border-radius: 6px; padding: 4px 12px; color: #94a3b8; font-size: 0.8rem; text-align: center; border: 1px solid #334155; }
.browser-body { flex: 1; background: #ffffff; overflow: hidden; }
.browser-body iframe { width: 100%; height: 100%; border: none; display: block; }

/* 手机外壳 */
.phone-frame {
  width: 280px; height: 85%; max-height: 560px; background: #0f172a;
  border-radius: 40px; border: 6px solid #1e293b; box-shadow: 0 20px 60px rgba(0,0,0,0.6);
  padding: 12px; display: flex; flex-direction: column; flex-shrink: 0;
}
.phone-notch { width: 120px; height: 20px; background: #0f172a; border-radius: 0 0 12px 12px; margin: -12px auto 8px; z-index: 2; }
.phone-screen { flex: 1; background: #ffffff; border-radius: 24px; overflow: hidden; }
.phone-screen iframe { width: 100%; height: 100%; border: none; display: block; }

.mockup-placeholder { width: 100%; height: 100%; min-height: 300px; background: #1e293b; border-radius: 16px; display: flex; justify-content: center; align-items: center; color: #94a3b8; border: 2px dashed #334155; }

/* --- 页码 --- */
.works-page-num { position: absolute; bottom: 2rem; right: 3rem; font-size: 0.8rem; color: #475569; letter-spacing: 1px; }

/* --- 骨架屏 --- */
.skeleton-container { height: 100vh; overflow-y: scroll; scroll-snap-type: y mandatory; }
.skeleton-section { height: 100vh; scroll-snap-align: start; display: flex; justify-content: center; align-items: center; padding: 2rem 5%; background: #0b0d10; }
.skeleton-section:nth-child(odd) { background: #111419; }
.skeleton-content { display: flex; flex-wrap: wrap; justify-content: center; align-items: center; gap: 2.5rem; max-width: 1300px; width: 100%; height: 85vh; }
.skeleton-info { flex: 1 1 35%; min-width: 280px; max-width: 500px; }
.skeleton-title { height: 3.5rem; width: 70%; background: #1e293b; border-radius: 8px; margin-bottom: 0.8rem; }
.skeleton-subtitle { height: 1.5rem; width: 50%; background: #1e293b; border-radius: 4px; margin-bottom: 1rem; }
.skeleton-desc { height: 5rem; width: 100%; background: #1e293b; border-radius: 4px; margin-bottom: 1rem; }
.skeleton-tags { display: flex; gap: 0.6rem; margin-bottom: 1.2rem; }
.skeleton-tags span { display: inline-block; height: 1.8rem; width: 4rem; background: #1e293b; border-radius: 16px; }
.skeleton-actions { display: flex; gap: 0.8rem; }
.skeleton-actions span { display: inline-block; height: 2.5rem; width: 6rem; background: #1e293b; border-radius: 40px; }
.skeleton-mockup { flex: 1 1 50%; min-width: 320px; max-width: 750px; height: 100%; max-height: 650px; }
.skeleton-browser { width: 100%; height: 100%; background: #1e293b; border-radius: 16px; border: 1px solid #334155; }
.skeleton-title, .skeleton-subtitle, .skeleton-desc, .skeleton-tags span, .skeleton-actions span, .skeleton-browser {
  animation: pulse 1.5s ease-in-out infinite;
}
@keyframes pulse { 0% { opacity: 0.4; } 50% { opacity: 0.8; } 100% { opacity: 0.4; } }

/* --- 空状态 --- */
.works-empty-state { height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; background: #0b0d10; color: #e8edf3; padding: 2rem; text-align: center; }
.works-empty-state .empty-icon { font-size: 4rem; margin-bottom: 1rem; }
.works-empty-state h2 { font-size: 2rem; margin-bottom: 0.5rem; }
.works-empty-state p { color: #94a3b8; max-width: 600px; line-height: 1.8; margin-bottom: 1rem; }
.works-empty-state code { background: #1e293b; padding: 0.2rem 0.6rem; border-radius: 4px; color: #e8edf3; font-size: 0.9rem; }
.works-empty-state .empty-example { background: #111419; padding: 1.5rem 2rem; border-radius: 12px; border: 1px solid #334155; text-align: left; max-width: 600px; width: 100%; margin-top: 1rem; }
.works-empty-state .empty-example pre { background: #0b0d10; padding: 0.8rem 1rem; border-radius: 6px; overflow-x: auto; color: #cbd5e1; font-size: 0.85rem; border: 1px solid #1e293b; }

/* --- 响应式 --- */
@media (max-width: 900px) {
  .works-content { flex-direction: column-reverse; height: auto; gap: 1rem; padding: 1rem 0; }
  .works-section { height: auto; min-height: 100vh; padding: 2rem 1rem; }
  .works-mockup { width: 100%; max-height: 400px; min-height: 300px; }
  .phone-frame { width: 200px; max-height: 380px; }
  .works-page-num { bottom: 1rem; right: 1.5rem; }
}
```

---

## 5. 实施清单

请按以下顺序操作，保证零失误：

- [ ] **备份当前代码**（建议新建分支 `git checkout -b feature/works-refactor`）。
- [ ] 创建 `scripts/generate-works.js` 并粘贴代码。
- [ ] 在 `package.json` 中添加 `"generate-works": "node scripts/generate-works.js"`。
- [ ] 创建 `public/works-data/` 文件夹。
- [ ] 把你现有的 `works/webrtc-live` 移动到 `public/works-data/webrtc-live`，并为其创建 `meta.json`。
- [ ] 创建一个示例项目 `public/works-data/demo-project/`，放入 `meta.json` 和 `preview.html`（测试用）。
- [ ] 运行 `npm run generate-works`，检查 `public/works-data/manifest.json` 是否生成。
- [ ] 替换 `src/pages/Works.jsx` 内容。
- [ ] 替换 `src/styles/pages/works.css` 内容。
- [ ] 运行 `npm run dev`，访问 `/works` 查看效果。
- [ ] 如有异常，检查 `manifest.json` 路径是否正确（浏览器开发者工具 Network 面板）。
- [ ] 确认无误后，删除旧的 `src/data/works.js`（可选，如无其他地方引用）。

---

## 6. 日常维护流程（以后加项目只需三步）

1. **新建文件夹**：`public/works-data/新项目名/`
2. **添加两个文件**：
   - `meta.json`（填写标题、描述、技术栈等）
   - `preview.html`（编写你的交互式 UI 演示）
3. **刷新清单**：运行 `npm run generate-works`，刷新网页即可。

> **永远不需要再改 React 代码，也不需要重新编译整个项目。**

---

## 7. 进阶建议：让 `preview.html` 更真实

- **模拟 API 请求**：在 `preview.html` 中用 `setTimeout` 模拟网络延迟，展示 Loading 状态。
- **模拟数据填充**：直接在前端写死几组演示数据（如列表、图表），展示视觉效果。
- **响应式设计**：确保 `preview.html` 在手机模拟器（280px 宽）和浏览器模拟器（750px 宽）下都适配良好。
- **引入图标库**：可以用 CDN 引入 Font Awesome 或 Lucide 图标，提升视觉精致度。
- **禁止真实后端请求**：`preview.html` 是纯前端演示，不要发起真实的外部 API 请求（避免跨域、认证等问题），如需展示数据交互，用 Mock 数据替代。

---

这份方案已经涵盖了从**架构设计**到**代码落地**的全部内容，你可以直接交给 AI 辅助执行，或者自己按照清单逐步实现。改造完成后，你的个人网站作品集将具备专业级的沉浸展示效果。🎉