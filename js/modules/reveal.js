export function initReveal() {
    // Scroll-reveal animation has been disabled to keep sections visible
    // immediately on load and to avoid hash-anchor scrolling issues.
}

export function destroyReveal() {
    // no-op
}
